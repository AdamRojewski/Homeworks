package JVMProject;

/*public class JVMTasks {
    public static void main(String[] args) {

        firstJVM(10, 20);
        AnotherCalculator("dodaj");
        JVMCalculator("DODAJ", 1, 2);
    }

    public static void firstJVM(int firstNumber, int secondNumber) {
        int sum = firstNumber + secondNumber;
        StringBuilder stringBuilder = new StringBuilder();
        stringBuilder.append("The sum is ");
        stringBuilder.append(sum);
        System.out.println(stringBuilder.toString());
    }
    private static void AnotherCalculator(String args) {
        String typeOfOperation = args[0];
        String firstOperand = args[1];
        int lenght = args.length();
        System.out.println();

        if (typeOfOperation.equals("dodaj")) {
            int first = Integer.valueOf(firstOperand);
            for (int i = 2; i <= lenght; i++) {
                first += Integer.valueOf(args[i]);
            }
            System.out.println(first);


        } else if (typeOfOperation.equals("odejmij")) {
            int first = Integer.valueOf(firstOperand);
            for (int i = 2; i <= lenght; i++) {
                first -= Integer.valueOf(args[i]);
            }
            System.out.println(first);

        }
    }
    private static void JVMCalculator(String task, int firstNumber, int secondNumber) {
        if (task == "DODAJ") {
            System.out.println(firstNumber + secondNumber);
        } else if (task == "ODEJMIJ") {
            System.out.println(firstNumber - secondNumber);
        } else if (task == "POMNOZ") {
            System.out.println(firstNumber * secondNumber);
        } else if (task == "PODZIEL") {
            System.out.println(firstNumber / secondNumber);
        } else System.out.println("Wpisz jedna z operacji: Dodaj / Odejmij / Pomnoz / Podziel");
    }
}
*/